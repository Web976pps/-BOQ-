{
  "name": "-BOQ- dev",
  "image": "python:3.11-slim",
  "postCreateCommand": "apt-get update && apt-get install -y tesseract-ocr tesseract-ocr-eng poppler-utils ghostscript && pip install -r requirements.txt",
  "customizations": {
    "vscode": {
      "extensions": [
        "github.copilot",
        "charliermarsh.ruff",
        "ms-python.python"
      ]
    }
  }
}
