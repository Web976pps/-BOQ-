import re

_CODE_PATTERN = re.compile(r'\b(CH|TB|C|SU|KT|SK|FL)\s*[\dA-Z-]+\b', re.IGNORECASE)

def detect(img, page_num):
    # Placeholder for detection logic using _CODE_PATTERN
    pass

def normalise_code(code):
    # Placeholder
    pass
